// Hapus import karena kita tidak lagi menggunakan data dari file terpisah
// import { projects } from '../data/projects.js';

export class PortfolioManager {
    constructor() {
        // Tidak perlu menyimpan projects karena kita menggunakan HTML statis
    }

    displayProjects() {
        // Tidak perlu melakukan apa-apa karena proyek sudah ada di HTML
    }
}

// Tidak perlu event listener karena proyek sudah ditampilkan di HTML